services:
  - type: web
    name: kaggriculture-mcts-agent
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3010
    healthCheckPath: /api/health
    autoDeploy: true
